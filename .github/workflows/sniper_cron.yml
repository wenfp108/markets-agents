name: 🧊 Routine System Maintenance  # 🎯 改为枯燥的名称：例行系统维护

on:
  schedule:
    - cron: '15 * * * *'  # 保持每小时 15 分运行
  workflow_dispatch:      # 允许手动强制点火

jobs:
  maintenance_task:
    runs-on: ubuntu-latest
    steps:
      - name: System Health Check and Pulse  # 🎯 改为：系统健康检查与脉冲
        run: |
          # 🔒 移除了带有“Sniper”或“Scouter”字样的日志
          echo "Initializing pulse sequence..."
          
          # 🎯 依然指向您的 Hugging Face 空间
          # -s: 静默模式，不显示进度条
          # -L: 自动跟随重定向
          curl -s -L -X GET "https://wenfp108-poly-data.hf.space/run"
          
          echo "✅ Sequence completed. Status: Operational."
